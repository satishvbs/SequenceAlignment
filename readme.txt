This is a simple Sequence aligner.

Import the files in your Java environment and run. 

You are supposed to input two amino acid sequences and a gap penalty score.

Then you will select the substitution matrix (BLOSUM 62 or PAM 120).

Hit the button and see the results.